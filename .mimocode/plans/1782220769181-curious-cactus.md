# Адаптация фронтенда под бэкенд

## Из FRONTEND_CHANGES.md — по порядку

### 1. Ticket → Task
- `packages/types/src/index.ts`: TicketType→TaskType, TicketStatus→TaskStatus, TicketItem→TaskItem, Ticket→Task
- TaskStatus: убрать `accepted`, добавить `cancelled`
- sentAt → createdAt

### 2. House + GuestSession
- House: убрать guestCount/lang/checkInAt, добавить deviceToken
- Новый GuestSession: id, houseId, guestCount, lang, checkInAt, checkOutAt, isActive

### 3. MenuItem
- hidden → isAvailable (инвертировать)
- Убрать showPrice, translations

### 4. Service
- ServiceFieldConfig → jsonSchema
- Добавить requiresTime, priceInfo

### 5. Message.sender
- 'guest'|'admin'|AssignedRole → 'GUEST'|'STAFF'

### 6. Удалить TransferDestination

### 7. Добавить MealType, Settings

### 8. WS events: ticket:* → task:*

## Верификация
- `npx tsc --noEmit` — 0 ошибок
